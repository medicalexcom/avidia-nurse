{
  "name": "@avidia/assessment",
  "version": "0.1.0",
  "private": true,
  "description": "Nursing question and assessment engine (M7): strict structured-output validation for AI question generation, the clinical validation pipeline (correctness, safety, distractor quality), deterministic scoring, content-hash deduplication, the provider-independent generation gateway, and repeatable generation-quality evaluation fixtures. Server-side only — never in the client bundle.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@avidia/ai-router": "workspace:*",
    "@avidia/domain": "workspace:*"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@babel/preset-typescript": "^7.26.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.20.1",
    "jest": "^29.7.0",
    "typescript": "~6.0.3"
  }
}
