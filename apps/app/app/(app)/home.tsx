import { TodayScreen } from '../../src/features/today/screens/TodayScreen';

/** Home is the Today screen (M9 spec A): action-first, START TODAY central. */
export default function HomeRoute() {
  return <TodayScreen />;
}
