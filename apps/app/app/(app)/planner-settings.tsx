import { PlannerSettingsScreen } from '../../src/features/planner/screens/PlannerSettingsScreen';

/** Availability + reminder preferences (M13 spec B/AA/AB). */
export default function PlannerSettingsRoute() {
  return <PlannerSettingsScreen />;
}
