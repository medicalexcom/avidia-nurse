import { PlannerScreen } from '../../src/features/planner/screens/PlannerScreen';

/** Study planner (M13 spec V/W/X): today's plan, week ahead, exam countdowns. */
export default function PlannerRoute() {
  return <PlannerScreen />;
}
