{
  "name": "@avidia/knowledge",
  "version": "0.1.0",
  "private": true,
  "description": "Nursing concept and knowledge layer (M6): deterministic concept-name normalization and deduplication, strict structured-output validation for AI concept extraction, the provider-independent concept-extraction gateway, and repeatable extraction-quality evaluation fixtures. Server-side only — never in the client bundle.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@avidia/domain": "workspace:*",
    "@avidia/rag": "workspace:*"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@babel/preset-typescript": "^7.26.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.20.1",
    "jest": "^29.7.0",
    "typescript": "~6.0.3"
  }
}
