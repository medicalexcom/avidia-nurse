{
  "name": "@avidia/analytics",
  "version": "0.1.0",
  "private": true,
  "description": "Learning analytics, readiness, and performance intelligence (M12): a pure, deterministic READ MODEL over the structured evidence produced by M6-M11. Computes mastery distributions, trends, confidence calibration, exam readiness with honest uncertainty, study consistency, mode and simulation analytics, deterministic error patterns, and actionable insights - all by CONSUMING the authoritative M8 mastery engine (@avidia/mastery), never by re-deriving mastery, priority, or scheduling. Pure functions only - no database access, no AI calls, no node built-ins - safe for the web bundle.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@avidia/domain": "workspace:*",
    "@avidia/mastery": "workspace:*"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@babel/preset-typescript": "^7.26.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.20.1",
    "jest": "^29.7.0",
    "typescript": "~6.0.3"
  }
}
